$(document).ready(function () {
  $('button').click(function () {
    // $('*').hide(); hides all elements
    // $(this).hide(); selects current html element
    $(':button').hide(); // selects all button elements
  });
});
